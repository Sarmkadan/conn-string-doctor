using System.Text.Json;

namespace ConnStringDoctor
{
    /// <summary>
    /// Provides JSON serialization helpers for <see cref="ConnectionStringRedactor"/>.
    /// </summary>
    public static class ConnectionStringRedactorJsonExtensions
    {
        // The static type <see cref="ConnectionStringRedactor"/> cannot be serialized directly.
        // Therefore, no JSON helper methods are implemented here.
    }
}
